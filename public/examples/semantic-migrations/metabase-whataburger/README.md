# Synthetic Metabase Whataburger benchmark

This is a hand-authored, non-production Metabase API snapshot used only for deterministic parser and engine comparison. It contains no customer data. MBQL, permissions, subscriptions, and rendering still require review and live validation.
